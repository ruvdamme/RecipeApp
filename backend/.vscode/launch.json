{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch with Make",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "preLaunchTask": "make & run"
        }
    ]
}